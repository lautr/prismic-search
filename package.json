{
  "name": "prismic-search",
  "version": "1.0.0",
  "description": "the missing prismic backend search",
  "author": "Johannes Lauter",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "test": "jest",
    "now-build": "nuxt build --spa"
  },
  "lint-staged": {
    "*.{js,vue}": "eslint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "dependencies": {
    "@nuxtjs/vuetify": "1.9.0",
    "nuxt": "2.9.2",
    "prismic-dom": "2.1.0",
    "prismic-javascript": "2.1.1"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "1.1.2",
    "@nuxtjs/eslint-module": "1.1.0",
    "babel-eslint": "10.0.3",
    "eslint": "6.6.0",
    "eslint-plugin-nuxt": "0.5.0",
    "husky": "3.1.0",
    "lint-staged": "9.4.3"
  }
}
